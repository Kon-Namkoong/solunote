<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vol.solunote.mapper.division.DivisionMapper">
                       
    <select id="checkDivision" resultType="int">
        SELECT COUNT(*) as CNT
        FROM TB_CS_INFO_DIVISION 
        WHERE division = #{division}
    </select>  
    
    
    <insert id="divisionRes">
        INSERT TB_CS_INFO_DIVISION (
        division, army_name) 
        VALUES (
        #{division},#{armyName});
    </insert>    

    <select id="divisionInfo" resultType="Map">
		select 
				SEQ as seq
                ,division  as division
                ,army_name as    armyName	 		
      	from TB_CS_INFO_DIVISION
		where division =    #{division}   		
    </select>       
    
    <update id="changeDivisionInfo"  > 
		UPDATE TB_CS_INFO_DIVISION
      	SET   
				army_name = #{armyNameChange}
				,division = #{division}
				,updated_at = CURRENT_TIMESTAMP
       WHERE SEQ = #{seq}
    </update>       
    
    <delete id="deleteDivisionInfo" >
		DELETE FROM TB_CS_INFO_DIVISION 
		WHERE division in 
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
	        #{item}
	    </foreach>
    </delete>    
    
</mapper>